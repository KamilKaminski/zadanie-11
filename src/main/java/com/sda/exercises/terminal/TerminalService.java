package com.sda.exercises.terminal;

public class TerminalService {

    public String writeOnTerminal() {
        String line = "";
        for (int i = 0; i<number; i++) {
            StringBuilder.append(character);
        }
}
